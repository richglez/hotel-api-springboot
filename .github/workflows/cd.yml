#name: CD Pipeline
#
#on:
#  push:
#    branches: [main]   # Solo se activa cuando llega código a main
#
#jobs:
#  deploy:
#    name: Deploy a Producción
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: Checkout código
#        uses: actions/checkout@v4
#
#      - name: Login a Docker Hub
#        uses: docker/login-action@v3
#        with:
#          username: ${{ secrets.DOCKERHUB_USERNAME }}
#          password: ${{ secrets.DOCKERHUB_TOKEN }}
#
#      - name: Build y Push imagen
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          push: true   # Ahora sí hacemos push al registry
#          tags: |
#            tuusuario/hotel-api:latest
#            tuusuario/hotel-api:${{ github.sha }}
#
#      # Ejemplo si tu servidor es un VPS con SSH
#      - name: Deploy al servidor
#        uses: appleboy/ssh-action@v1
#        with:
#          host: ${{ secrets.SERVER_HOST }}
#          username: ${{ secrets.SERVER_USER }}
#          key: ${{ secrets.SERVER_SSH_KEY }}
#          script: |
#            cd /app/hotel-api
#            docker compose pull
#            docker compose up -d --no-build